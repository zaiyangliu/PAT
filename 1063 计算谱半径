#include<cstdio>
#include<cmath>
int main(){
  int N;
  scanf("%d", &N);
  double Max = -1.0;
  for(int i = 0; i < N; ++i){
    double temp;
    int a, b;
    scanf("%d%d", &a, &b);
    temp = sqrt(1.0 *( a * a + b * b) );
    if(temp > Max) Max = temp;
  }
  printf("%.2lf", Max);
  return 0;
}
